name: Win - Manage steps workflow 
on: workflow_dispatch

jobs:
  quality:
    name: Lorem Ipsum
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.4
      - name: Run script
        run: |
          /scripts/script.bat
        
